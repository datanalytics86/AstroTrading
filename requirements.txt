# AstroTrading MVP — Python 3.11+
# Build tooling (needed for editable install on Streamlit Cloud)
setuptools>=68
wheel

# Install local package (src/astrotrading) so `import astrotrading` works on Cloud
-e .

skyfield>=1.49
jplephem>=2.22
numpy>=1.26
pandas>=2.1
scipy>=1.11
yfinance>=0.2.40
plotly>=5.18
streamlit>=1.32
python-dotenv>=1.0
httpx>=0.27
bcrypt>=4.1
pyyaml>=6.0
# Parquet cache for market data (CSV fallback if missing)
pyarrow>=14.0
